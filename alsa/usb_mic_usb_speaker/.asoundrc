pcm.!default {
  type asym
  capture.pcm "usb_mic"
  playback.pcm "usb_speaker"
}

pcm.usb_mic {
  type plug
  slave {
    pcm "hw:1,0"
    rate 48000
  }
}

pcm.usb_speaker {
  type plug
  slave {
    pcm "hw:2,0"
    rate 48000
  }
}
